# AI-лог разработки

Сырьё для раздела README «AI в разработке». Одна строка на задачу, заполняется сразу после неё.
Инструмент по умолчанию — Claude Code; если другой, указывается в колонке «Сгенерировано AI».

| Шаг | Задача | Сгенерировано AI | Переписано / решено руками и почему |
| --- | --- | --- | --- |
| 0 | Стек, правила, ADR 001–005, план | Claude Code: черновики `CLAUDE.md`, ADR, `docs/plan.md` по заданию, макету и списку технологий от автора | Автор задал стек (React, Vite, TS, zod, react-query, WebSocket, пересчёт вверх по нодам, exponential backoff, styled-components без дизайн-системы), нарисовал макет и схему деплоя, выбрал процесс «ветка от ветки». _Правки после вычитки — дописать._ |
| 0 | Ревизия ADR 004: рестарт сервера | Claude Code: `epoch` (id запуска) в `ETag`, `hello`/`patch`/`reset` и модели; правило «эпоха сравнивается первой»; `cancelRefetch: false` | Проблему нашёл автор при вычитке: in-memory `version` после падения сервера обнуляется, и сравнение одних номеров версий даёт тихое расхождение (совпавший `hello`/`ETag`, патчи режутся как дубликаты). |
