# AI-лог разработки

Сырьё для раздела README «AI в разработке». Одна строка на задачу, заполняется сразу после неё.
Инструмент по умолчанию — Claude Code; если другой, указывается в колонке «Сгенерировано AI».

| Шаг | Задача                                                            | Сгенерировано AI                                                                                                                                                                                                                                                      | Переписано / решено руками и почему                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0   | Стек, правила, ADR 001–005, план                                  | Claude Code: черновики `CLAUDE.md`, ADR, `docs/plan.md` по заданию, макету и списку технологий от автора                                                                                                                                                              | Автор задал стек (React, Vite, TS, zod, react-query, WebSocket, пересчёт вверх по нодам, exponential backoff, styled-components без дизайн-системы), нарисовал макет и схему деплоя, выбрал процесс «ветка от ветки». _Правки после вычитки — дописать._                                                                                                                                                                                                                                                                                                                                |
| 0   | Ревизия ADR 004: рестарт сервера                                  | Claude Code: `epoch` (id запуска) в `ETag`, `hello`/`patch`/`reset` и модели; правило «эпоха сравнивается первой»; `cancelRefetch: false`                                                                                                                             | Проблему нашёл автор при вычитке: in-memory `version` после падения сервера обнуляется, и сравнение одних номеров версий даёт тихое расхождение (совпавший `hello`/`ETag`, патчи режутся как дубликаты).                                                                                                                                                                                                                                                                                                                                                                                |
| 0   | Перепроверка плана перед стартом шага 1                           | Claude Code: сверка макета по координатам, проверка версий по реестру npm, независимая техническая рецензия плана (react-query v5, ETag/304, source-only TS-пакет, перезапуск CSS-анимации, `grid-template-rows`), правки в `CLAUDE.md`, ADR 001–005 и `docs/plan.md` | Инициатива автора («перепроверь план»). Автор же принял три продуктовых решения, которых в плане не было: поиск фильтрует и дерево, и таблицу с подсветкой совпадений; «Штат» узла — «3 / 128»; клавиатура по дереву переносится на шаг 3. Найдено и исправлено до написания кода: `applyPatch → null` отравил бы кэш через `setQueryData`; подсветка через `key` мигала бы на первой отрисовке; `cancelRefetch` стоял не в том аргументе; обоснование `structuralSharing: false` было фактически неверным; `typescript@latest` (7.x) и `eslint@latest` (10.x) несовместимы с линтером. |
| 1   | Scaffold, контракты, сервер, модель, слой API                     | Claude Code: монорепо и конфигурация инструментов, строгие zod-схемы, детерминированный сид на 61 узел, `ETag` с ревизией и `304`, `buildModel` с проверкой целостности, `fetchOrgTree`/`loadOrgModel`, тесты (122 шт.)                                               | Версии пиньены по факту проверки реестра: `typescript@latest` (7.x) не поддерживается `typescript-eslint`, `eslint-plugin-react` не поддерживает ESLint 10 — плагин убран, запрет inline-CSS сделан правилом ядра. Тема перенесена из `app` в `shared/config`: `shared` не имеет права импортировать `app`.                                                                                                                                                                                                                                                                             |
| 1   | Тема, примитивы, дерево, состояния                                | Claude Code: токены и `motion`, `PerformanceBar`, стор на `useSyncExternalStore`, дерево с `memo` по селектору, четыре экранных состояния, компонентные тесты                                                                                                         | Отступ уровня перенесён с вложенного `ul` на ячейку имени — иначе колонки макета не выравниваются (ADR 005 обновлён). Экран ошибки не появлялся вообще: выяснилось запуском приложения, что react-query выставляет `error` только после исчерпания ретраев, а в неактивной вкладке ставит их на паузу; добавлено чтение `failureReason`. По пути отброшена собственная неверная гипотеза про `networkMode`.                                                                                                                                                                             |
| 2   | Агрегация, форматирование                                         | Claude Code: аддитивный агрегат с числителем взвешенного среднего, обход по убыванию глубины без рекурсии, обязательный unit-тест агрегации, ru-форматирование                                                                                                        | Проверка «взвешенное ≠ простое среднее» добавлена намеренно: без неё тест прошёл бы и на неверной формуле.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2   | Фильтр, строки, таблица, поиск, выделение                         | Claude Code: `selectFilteredView` (совпадения + предки), `selectRows`, таблица с сортировкой по ADR 006, поиск с дебаунсом, общий стор выделения, split-view                                                                                                          | Два дефекта нашлись запуском, а не тестами: отступ уровня в таблице перебивался общим правилом `td` (лечится `&&`), и у поля `type="search"` рядом с собственным крестиком появлялся родной. Порядок `null`-значений при сортировке по убыванию поймал тест.                                                                                                                                                                                                                                                                                                                            |
| 2   | Прокрутка таблицы к выделенному узлу, колонка «Всего сотрудников» | Claude Code: эффект на `selectedId` со `scrollIntoView` следующим кадром, `scroll-margin` под липкую шапку, тесты                                                                                                                                                     | Обе правки — по замечаниям автора: прокрутка работала только из таблицы в дерево, а в колонке «Всего сотрудников» показывались два числа вместо суммы.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 3   | Контракт WS, сервер, поток                                        | Claude Code: схемы `hello/patch/reset/ping`, рассылка патчей и heartbeat, мутации стора, детерминированный тикер, dev-эндпоинт обрыва                                                                                                                                 | Тест ловли сообщений пришлось переписать на буфер: `hello` уходит в момент подключения, и слушатель, повешенный после `open`, его не застаёт.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 3   | `applyPatch`, backoff, автомат соединения                         | Claude Code: дельта вверх по предкам, property-тест «инкремент ≡ полный пересчёт» на сиде, backoff с полным джиттером, конечный автомат с heartbeat и сбросом попыток                                                                                                 | Осознанно инжектируются сокет, часы и random — иначе тесты были бы флаки, а поведение backoff непроверяемо.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 3   | Подсветка, клавиатура, анимация                                   | Claude Code: `useFlash` на чередовании двух анимаций, общий roving tabindex, `grid-template-rows` + синхронный `inert`                                                                                                                                                | Клавиатура дважды не работала, и оба раза причину нашли тесты: активный элемент брался из состояния, отстающего от фокуса, и фокус ставился из эффекта, который не запускался при совпадении значения. Рефетч по фокусу окна отключён — при живом сокете он давал лишние запросы.                                                                                                                                                                                                                                                                                                       |
